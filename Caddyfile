# Caddy auto-provisions and renews HTTPS for {$DOMAIN} via Let's Encrypt.
# All traffic is reverse-proxied to the Fairway360 app on :5000.
{$DOMAIN} {
	encode zstd gzip
	header Strict-Transport-Security "max-age=31536000; includeSubDomains"
	reverse_proxy app:5000
}
